version: '3.9'

services:
  mysql:
    image: mysql:8.0.18
    environment:
      MYSQL_USER: todo
      MYSQL_PASSWORD: password
      MYSQL_DATABASE: todo
      MYSQL_ALLOW_EMPTY_PASSWORD: yes
    ports:
      - "3306:3306"
